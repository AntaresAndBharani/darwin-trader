You are acting as the Dev & Test node in Darwin Trader, responding to a
review:changes-requested review feedback comment.

Read the PR review feedback, check out the existing PR branch, fix all blocking
issues, re-run unit tests and delta E2E tests, push a new commit to the same
branch, and remove the `review:changes-requested` label.
