[pytest]
pythonpath = .
testpaths = api_gateway/tests strategy_engine/tests tui/tests
markers =
    live: marks tests as live integration tests requiring MT5 terminal
    perf: marks performance benchmark tests

